#include <stdio.h>

int main() {
    char ch;

    // Input a single character
    printf(" ");
    scanf("%c", &ch);

    // Print the ASCII value of the character
    printf("ASCII Value: %d\n", ch);

    return 0;
}