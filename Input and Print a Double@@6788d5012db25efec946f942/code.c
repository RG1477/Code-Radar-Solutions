#include <stdio.h>

int main() {
    double number;

    // Read the double-precision floating-point number
    scanf("%lf", &number);

    // Print the entered number
    printf("You entered: %lf\n", number);

    return 0;
}